package frc.robot.utilities;

import org.junit.Assert;

import org.junit.Test;

import frc.robot.commands.kickercommands.RunKickerCommand;
import frc.robot.subsystems.KickerSubsystem;

public class HopperEncoderTest {
    
    @Test
    public void testSet1() {
        //RunKickerCommand runKickerCommand = new RunKickerCommand();

        //for(double i = 0.0; i < 1.0; i += 0.01)
        //{
            //System.out.print("index: " + i);
            //Assert.assertEquals(true, runKickerCommand.calculateStart(0.91));
            // Assert.assertTrue(runKickerCommand.calculateStart(0.81));
            // Assert.assertFalse(runKickerCommand.calculateStart(0.91));
            // Assert.assertTrue(runKickerCommand.calculateStart(1.81));
            // Assert.assertFalse(runKickerCommand.calculateStart(1.91));
            // Assert.assertFalse(runKickerCommand.calculateStart(0.5));
            // Assert.assertTrue(runKickerCommand.calculateStart(0.4));
            // Assert.assertFalse(runKickerCommand.calculateStart(15.5));
        //}
    }
}